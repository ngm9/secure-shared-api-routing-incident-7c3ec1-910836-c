apiVersion: v1
kind: Service
metadata:
  name: api1
  namespace: api1
  labels:
    app: api1
spec:
  type: ClusterIP
  selector:
    app: api1
  ports:
    - name: http
      port: 80
      targetPort: 8080
