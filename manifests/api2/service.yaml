apiVersion: v1
kind: Service
metadata:
  name: api2
  namespace: api2
  labels:
    app: api2
spec:
  type: ClusterIP
  selector:
    app: api2
  ports:
    - name: http
      port: 80
      targetPort: 8080
